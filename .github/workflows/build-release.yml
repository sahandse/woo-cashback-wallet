name: Build Plugin ZIP

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare plugin
        run: |
          mkdir -p build/woo-cashback-wallet
          rsync -av --exclude='.git' --exclude='.github' --exclude='build' ./ build/woo-cashback-wallet/

      - name: Create ZIP
        run: |
          cd build
          zip -r woo-cashback-wallet.zip woo-cashback-wallet

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: woo-cashback-wallet
          path: build/woo-cashback-wallet.zip

      - name: Publish GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: build/woo-cashback-wallet.zip
          generate_release_notes: true
